#include <sys/types.h>
#include <sys/stat.h>
#include <stdio.h>
#include <stdlib.h>
#include <fcntl.h>
#include <errno.h>
#include <unistd.h>
#include <syslog.h>
#include <string.h>
#include <time.h>

int main()
{
	
	int b_ultah = 66120;
        int ultah = 80520;
        int flag1 = 1, flag2 = 1;
	while(1) {
		time_t t = time(NULL);
		struct tm tm = *localtime(&t);
		
		if((tm.tm_mon + 1 == 4) && (tm.tm_mday == 9)) {
		
			if (((tm.tm_hour*3600 + tm.tm_min*60) >= b_ultah) && (flag1 == 1)) {
				flag1 = 0;
				printf("6 jam sebelum ultah");
			}
			
			if (((tm.tm_hour*3600 + tm.tm_min*60) >= ultah) && (flag2 == 1)) {
				flag2 = 0;
				printf("ultah bro");
			}
		}
		sleep(10);
        }
        return 0;
}